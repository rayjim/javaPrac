class Personel{

	private Person peoRoot;
	int nNum;
        int id;
	
        Personel()
	{
		personTree =null;
		nNum = 0;
		id = 1;
	
	}
	public boolean insert(String lastName, String firstName, int age)
 	{
	 	if (peoRoot==null)
		peoRoot(id, lastName, firstName, age);
		else
		{
			if()
		
		}
	
	}

}
